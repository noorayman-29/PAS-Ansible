---
- hosts: managed_pas_hosts
  gather_facts: false

  # The variables you would most likely want/need to override have been included
  vars:

    # Join
    join_server: ubuntu-pas-pm
    join_state: joined
    join_password: Admin123
    join_extra_args: ''

    # Facts
    join_facts_generate: true
    join_facts_verbose: false

    # Reports
    join_reports_generate: true
    join_reports_backup: true
    join_reports_host: awx-itg
    join_reports:
    - src:  join_report.csv.j2
      dest: /home/reports/pas/pas_join_report.csv
    - src:  join_report.html.j2
      dest: /home/reports/pas/pas_join_report.html

  roles:
    - name: oneidentity.privilege_manager.join
